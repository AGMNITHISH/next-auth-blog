export interface inputInterface {
  label: string;
  placeholder?: string;
  name: string;
  value?: string;
  className?: string;
}
export interface textAreaInterface {
  label: string;
  placeholder?: string;
  name: string;
}
